[pytest]
testpaths = tests
addopts = -ra -m "not integration"
markers =
    integration: tests qui exigent le venv complet (Airflow, bp2i) ; lancer avec COS_TESTS_FORCE_STUBS=0 -m integration
xfail_strict = false
